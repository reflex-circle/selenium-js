dependencies:
  override:
   - npm -g install npm 
   - npm -g install selenium-webdriver
   - npm -g install mocha
   - npm -g install expect.js

test:
  pre:
   - java -jar selenium-server-standalone-2.53.3.jar -Dwebdriver.chrome.driver=chromedriver
  override:
   - echo Hello World
   - mocha googleTest.js --timeout 10000
